@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewData["Title"] = "Roles List";
}

<style>
    .table td, .table th {
        text-align: center;
    }

    /* Add horizontal lines between rows */
    .table tbody tr {
        border-bottom: 2px solid #dadada; /* Bootstrap's default border color */
    }

    /* Add border to the entire table */
    .table {
        border: 2px solid #dadada; /* Customize the border color as needed */
        border-radius: 0.25rem; /* Optional: Rounded corners */
    }

    /* Border for the table header */
    .table thead th {
        border-bottom: 1.5px solid #dadada; /* Change the border color to gray */
    }
</style>

<div class="d-flex justify-content-between mb-3">
    <h4>Roles List</h4>
    <div class="d-flex justify-content-between gap-3">
        <a href="@Url.Action("Create", "Roles")" class="btn btn-primary btn-sm">Create Role</a>
    </div>
</div>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th scope="col">Role Name</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Name</td>
                <td>
                    <a href="@Url.Action("Edit", "Roles", new { id = role.Id })" class="btn btn-warning btn-sm">Edit</a>
                    <a href="@Url.Action("Delete", "Roles", new { id = role.Id })" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
